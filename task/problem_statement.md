# Problem Statement

Try to learn a neural network adding two binary numbers or performing multiplication by two
(note that result should also be some binary number).

In this repository, I focus on the first task:
training a neural network to add two binary numbers and return the result in binary form.